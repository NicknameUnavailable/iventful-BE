# ===================================
#  Form Field Definitions
# ===================================

fields:
    id:
        label: ID
        disabled: true
    user:
        label: User
        disabled: false
        type: relation
        select: name
    event:
        label: Following
        disabled: false
        type: relation
        select: name
